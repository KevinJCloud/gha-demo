name: hello world
on: 
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - name: first step
         shell: bash
         run: |
           echo "Welcome to GHA"
           ls -l
           pwd 
           echo "done"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: my test job
        run: echo "test has been completed"
