name: hello world
on: 
  workflow_dispatch:
  push:
    branches:
        -  main
        - 'feature/**'
  pull_request:
       branches:
         - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - name: first step
         shell: bash
         run: |
           echo "Welcome to GHA"
           ls -l
           pwd 
           echo "done"

