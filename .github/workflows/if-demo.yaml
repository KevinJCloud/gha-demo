name: if demo
on:
  push:
   branches:
      - feature/pr_demo

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: if demo eg
        if: github.ref == 'refs/heads/feature/pr_demo'
        run: echo "running this step from main branch"

      - name: simple step
        run: echo " runnning this simple step"
